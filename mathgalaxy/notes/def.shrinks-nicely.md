# 可缩族　`def.shrinks-nicely`　·　说明
根 `../`

两个条件的含义：**装得进去**（$E_r$ 在球里）、**不能太瘪**（体积至少是球的 $\alpha$ 倍）。

典型例子：取 $E_r = B(r, x)$ 本身（$\alpha = 1$）；或取 $E_r$ 为球内某个固定形状（如立方体）按比例缩放。

反例：$E_r$ 取成球内一条细长的薄片 —— 体积比可以趋于 0，这样的族就不是「可缩地」趋于 $x$。

⭐ 引入这个概念是为了让微分定理**不依赖球的形状**：只要不瘪，结论照旧。
