# 简单函数的积分　`def.integral-simple`　·　说明
根 `../`

这就是「面积 $=$ 各层高度 $\times$ 各层测度」—— 完全照着 Riemann 和的样子写，只是把区间换成了可测集。

要检查**良定义**：同一个 $\varphi$ 可以写成不同的标准形式（比如把某个 $E_{j}$ 再拆开），但由 $\mu$ 的有限可加性，结果一样。

约定 $0 \cdot \infty = 0$：这一步很关键 —— 它让「在无穷测度集上取 0 的函数」积分为 0，符合直觉。
