# 导数　`def.derivative`　·　说明
根 `../`

⭐ 微分理论关心的是**逐点**行为，所以定义域常常收缩到 $L^1_{loc}$ 而不是 $L^1$。

要点（本星图 BV / AC 那一段全靠它们）：
- 可导 $\implies$ 连续，反之不成立（$|x|$ 在 $0$ 处）。
- 单调函数**几乎处处可导**，且 $F' \ge 0$ a.e.。
- 可导**不保证** $F(b) - F(a) = \int_a^b F'$（Cantor 函数就是反例）；等式成立要加绝对连续。
