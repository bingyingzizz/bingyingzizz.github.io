# Borel σ-代数　`def.borel`　·　说明
根 `../`

开集族本身一般只是拓扑，不是 $\sigma$代数 —— 取它生成的 $\sigma$代数才得到 Borel 集全体。

$\mathbb{R}$ 上 $\mathfrak{B}_\mathbb{R}$ 也可以由半开区间 $\{ (a, b] : a < b \}$ 生成（或 { [a, b) }）。这些形式在构造测度时更好用，因为半开区间的「不交并」算起来干净。
