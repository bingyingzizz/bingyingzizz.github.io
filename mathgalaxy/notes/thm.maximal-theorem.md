# 极大定理　`thm.maximal-theorem`　·　说明
根 `../`

这叫**弱 (1,1) 型**不等式：不能保证 $Hf \in L^1$（事实上一般不是），但能保证它的**分布函数**被 $1/\alpha$ 控制。

在 $\mathbb{R}^{n}$ 里 $C = 3^n$（这正是覆盖引理里那个膨胀因子）。

⭐ 它的角色：**「平均值的例外集是小的」** —— 于是一切逐点收敛问题都可以用「例外集测度 $\to 0$」来解决。
