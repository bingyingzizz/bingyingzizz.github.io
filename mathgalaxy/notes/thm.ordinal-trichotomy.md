# 序数可比　`thm.ordinal-trichotomy`　·　说明
根 `../`

⭐ 三歧性使序数可以当「坐标」用：任意两个**良序集**的序型一定可比，而且**不需要选择公理**。

⚠ 这与「任意两个**基数**可比」完全是两件事，后者要用 AC（见「基数可比定理」）。差别在于序数**自带**一个良序 —— 就是 $\in$；而一般的集合没有先验的良序，要给它找一个，那正是良序定理（$\iff$ 选择公理）的内容。
