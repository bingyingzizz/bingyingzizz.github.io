# 正则 Borel 测度　`def.regular-measure`　·　说明
根 `../`

第二条是「从外面用开集逼近」。在 $\mathbb{R}^{n}$ 上它其实由第一条推出。

**每个正则测度都是 $\sigma$有限的**（用紧集的可数覆盖）。

$f \in L^+(\mathbb{R}^n)$ 时，$f\cdot dm$ 正则 $\iff$ $f \in L^1_{loc}$ —— 这条把「正则」与「局部可积」接了起来。
