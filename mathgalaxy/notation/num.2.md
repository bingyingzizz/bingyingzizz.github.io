# 记号表 · 数系与分析基础
根 `../`　·　索引 `notation.md`

*单调递增地趋于** — 集列同理：$E_n \uparrow X$；递减写 $\downarrow$
- `\nearrow, \searrow` ↗ ↘　**（集列 / 函数列）单调递增 / 递减**
- `\nrightarrow` ↛　**不趋于（$\int f_n = 1 \nrightarrow 0$）**
- `C \leftarrow C \cup \{ v \}` ←　**赋值：把左边更新成右边（＝「令…为…」）**

索引 `notation.md`。
