# 拓扑空间与开集　`def.topology`
拓扑空间 / 开集（Topological Space）
layer 4 · 定义 · 拓扑空间 · 拓扑学

设 $X$ 是集合，$\mathcal{T} \subseteq \mathcal{P}(X)$ 是 $X$ 的一族子集。$(X, \mathcal{T})$ 是一个**拓扑空间**，$\mathcal{T}$ 叫作 $X$ 上的一个**拓扑**，当且仅当：
> 陈述续见 `nodes/def.topology.2.md`


## 为什么成立（入边，证明在 proofs/）
- `def.power-set` 幂集 𝒫(X)：用到了定义 幂集 𝒫(X)　proofs/dep.powerset-topology.md
- `def.subset` 子集：用到了定义 子集　proofs/dep.subset-topology.md

## 它能推出什么 / 谁在用它
- 被 `def.closed-set` 闭集与闭包 用
- 被 `def.topology-base` 基与子基 用
- 被 `def.continuous-map` 连续映射 用
- 被 `def.metric-space` 距离空间 用
- 被 `def.borel` Borel σ-代数 用
- 被 `def.continuous` 连续 用

refs: Munkres, Topology, Ch. 2

> 说明见 `notes/def.topology.md`
