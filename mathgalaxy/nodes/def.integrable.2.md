# 可积 / L¹　`def.integrable`　·　续页（第 2 页）
根 `../`　·　第 1 页 `nodes/def.integrable.md`

## 它能推出什么 / 谁在用它（续）
- 被 `prop.L1-vector-space` L¹ 是向量空间 用
- 被 `thm.approximation-L1` L¹ 里的逼近 用
- ⇒ `prop.L1-implies-measure` L¹ 收敛 ⟹ 依测度收敛
- 被 `prop.L1-implies-measure` L¹ 收敛 ⟹ 依测度收敛 用
- 被 `thm.fubini-tonelli` Fubini–Tonelli 用
- 被 `prop.integral-gives-ac` 积分给出绝对连续测度 用
- 被 `thm.rn-complex` 复测度的 Radon–Nikodym 用
- 被 `prop.L1-signed` L¹(ν) 与全变差 用
- 被 `thm.ftc-lebesgue` 微积分基本定理（Lebesgue 版） 用
- 被 `prop.integral-abs-ineq` 积分绝对值不等式 用
- 被 `prop.L1-support-sigma-finite` L¹ 函数的支撑 σ-有限 用
- 被 `cor.ac-integral-continuity` 积分的绝对连续性 用
- 被 `prop.integrals-equal-iff` 何时两个函数积分处处相同 用
- 被 `thm.linf-properties` L^∞ 的性质 用
- 被 `prop.nbv-derivative` NBV 函数的导数与测度的关系 用
- 被 `cor.integral-is-ac-nbv` 积出来的函数是 AC · NBV 用
- 被 `def.convergence-modes` 五种收敛 用

> 续见 `nodes/def.integrable.3.md`
