# σ-代数　`def.sigma-algebra`
σ-代数（σ-Algebra）
layer 4 · 定义 · 集合族与 σ-代数 · 分析学

设 $X$ 是集合，$\mathfrak{A} \subseteq \mathcal{P}(X)$。$\mathfrak{A}$ 是 $X$ 上的一个 **$\sigma$代数**，当且仅当它同时满足三条：
> 陈述续见 `nodes/def.sigma-algebra.2.md`


## 为什么成立（入边，证明在 proofs/）
- `def.sigma-ring` σ-环：用到了定义 σ-环　proofs/def-link.sigmaalgebra-sigmaring.md

## 它能推出什么 / 谁在用它
- 被 `thm.ring-monotone-sigma` 环 → σ-环 的判据 用
- 被 `thm.monotone-class` 单调类定理 用
- 被 `def.generated-sigma` 生成的 σ-代数 用
- 被 `def.borel` Borel σ-代数 用
- 被 `def.product-sigma` 积 σ-代数 用
- 被 `def.measure` 测度 用
- 被 `def.measurable-function` 可测函数 用
- 被 `def.signed-measure` 符号测度 用
- 被 `def.complex-measure` 复测度 用

refs: Halmos, Measure Theory, §4；Folland, Real Analysis, §1.2；Rudin, Real and Complex Analysis, Ch. 1

> 说明见 `notes/def.sigma-algebra.md`
