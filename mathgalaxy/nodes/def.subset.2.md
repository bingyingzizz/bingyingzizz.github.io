# 子集　`def.subset`　·　续页（第 2 页）
根 `../`　·　第 1 页 `nodes/def.subset.md`

## 它能推出什么 / 谁在用它（续）
- 被 `def.chain` 链 用
- 被 `def.bound` 界与确界 用
- 被 `def.wellorder` 良序集 用
- 被 `def.vs` 向量空间的基 用
- 被 `def.fundamental-class` 基本类 用
- 被 `def.set-ring` 环与代数 用
- 被 `def.monotone-class` 单调类 用
- 被 `def.outer-measure` 外测度 用
- 被 `def.section` 截口 用
- 被 `def.shrinks-nicely` 可缩族 用
