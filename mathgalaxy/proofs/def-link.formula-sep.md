# def-link.formula-sep
`def-link.formula-sep` · 定义引用 · strong 边 · 根 `../`

`def.formula` 公式 → `ax.sep` 分离公理模式

分离公理模式写的是「对**每个公式** $\varphi (x, p)$（其中 $B$ 不出现），下面是一条公理」—— 它是公理**模式**而不是单条公理，正在于它要对每个公式都发一条。
