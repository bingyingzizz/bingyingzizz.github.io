# dep.topology-engine-continuous
`dep.topology-engine-continuous` · 定义引用 · strong 边 · 根 `../`

`def.topology` 拓扑空间与开集 → `def.continuous` 连续

$\varepsilon$-$\delta$ 那套是**拓扑版连续**在实数（或距离空间）上的展开形式：开球就是 $\varepsilon$ 邻域。
