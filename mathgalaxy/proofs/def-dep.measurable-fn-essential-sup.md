# def-dep.measurable-fn-essential-sup
`def-dep.measurable-fn-essential-sup` · 定义引用 · strong 边 · 根 `../`

`def.measurable-function` 可测函数 → `def.essential-sup` 本性上界与 L^∞

$L^\infty$ 的元素是**可测**且本性上界有限的函数。
