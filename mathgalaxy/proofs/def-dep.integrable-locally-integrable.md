# def-dep.integrable-locally-integrable
`def-dep.integrable-locally-integrable` · 定义引用 · strong 边 · 根 `../`

`def.integrable` 可积 / L¹ → `def.locally-integrable` 局部可积

判据 $\int_K |f| < \infty$ 与 $L^1$ 的可积判据同源，只是把整个空间换成有界可测集 $K$。
