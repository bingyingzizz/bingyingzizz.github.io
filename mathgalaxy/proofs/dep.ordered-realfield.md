# dep.ordered-realfield
`dep.ordered-realfield` · 定义引用 · strong 边 · 根 `../`

`def.ordered-field` 有序域 → `thm.real-ordered-field` ℝ 是完备有序域

要证的就是「构造出来的 $\mathbb{R}$ 是一个**完备有序域**」：前两组公理是「有序域」，第三组是不属于这条定义的额外一条。
