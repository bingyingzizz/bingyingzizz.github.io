# dep.topology-metric
`dep.topology-metric` · 定义引用 · strong 边 · 根 `../`

`def.topology` 拓扑空间与开集 → `def.metric-space` 距离空间

$d$ 诱导出的那族开集（开球之并）就是这条定义里的拓扑：三条公理逐条可验（任意并由球的定义封闭，有限交由三角不等式保证）。所以**每个距离空间都是拓扑空间**，反之不成立。
