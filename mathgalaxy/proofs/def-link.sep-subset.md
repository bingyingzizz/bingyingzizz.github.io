# def-link.sep-subset
`def-link.sep-subset` · 定义引用 · strong 边 · 根 `../`

`def.subset` 子集 → `ax.sep` 分离公理模式

分离公理模式给出的 $B = \{ x \in A : \varphi (x, p) \}$ 恰好是 $A$ 的一个**子集**——"只从 $A$ 里挑元素"正是 $\subseteq$ 的语义。
