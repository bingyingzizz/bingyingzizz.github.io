# def-link.measurable-function-section-measurable
`def-link.measurable-function-section-measurable` · 定义引用 · strong 边 · 根 `../`

`def.measurable-function` 可测函数 → `prop.section-measurable` 截口可测

**(b)** 说的正是「可测**函数**的截口仍可测」。
