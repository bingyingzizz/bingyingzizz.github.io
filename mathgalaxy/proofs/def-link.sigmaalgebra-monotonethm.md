# def-link.sigmaalgebra-monotonethm
`def-link.sigmaalgebra-monotonethm` · 定义引用 · strong 边 · 根 `../`

`def.sigma-algebra` σ-代数 → `thm.monotone-class` 单调类定理

单调类定理的两端，一个是由 $\mathcal{E}$ 生成的 $\sigma$代数 $\mathcal{M}(\mathcal{E})$、一个是单调类 $\mathfrak{m}(\mathcal{E})$。
