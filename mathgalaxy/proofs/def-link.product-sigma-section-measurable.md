# def-link.product-sigma-section-measurable
`def-link.product-sigma-section-measurable` · 定义引用 · strong 边 · 根 `../`

`def.product-sigma` 积 σ-代数 → `prop.section-measurable` 截口可测

前提 $E \in \mathcal{M} \otimes \mathcal{N}$ 里的 $\otimes$ 就是**积 $\sigma$-代数**。
