# def-link.ordinal-cardinal
`def-link.ordinal-cardinal` · 定义引用 · strong 边 · 根 `../`

`def.ordinal` 序数 → `def.cardinal` 基数 |A|

严格说来「等势类」是真类，通行做法是取每个等势类里最小的**序数**作代表 —— 那个序数才叫基数。（本图的 $|A|$ 只用到单射/双射，所以这条依赖是概念上的，不是定义上的。）
