# def-dep.sigma-algebra-measurable-fn
`def-dep.sigma-algebra-measurable-fn` · 定义引用 · strong 边 · 根 `../`

`def.sigma-algebra` σ-代数 → `def.measurable-function` 可测函数

可测空间 $(X, \mathcal{M})$ 就是「集合 $+$ $\sigma$ 代数」；可测性说的是原像落在 $\mathcal{M}$ 里。
