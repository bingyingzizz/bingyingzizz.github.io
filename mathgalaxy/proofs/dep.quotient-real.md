# dep.quotient-real
`dep.quotient-real` · 定义引用 · strong 边 · 根 `../`

`def.quotient-set` 商集与等价类 → `def.real` 实数系 ℝ

和 $\mathbb{Z}$、$\mathbb{Q}$ 是**同一招**：把已有对象按等价关系粘成等价类。整条链 $\mathbb{N} \to \mathbb{Z} \to \mathbb{Q} \to \mathbb{R}$ 用的都是这一个手法。
