# def-link.power-powerset
`def-link.power-powerset` · 定义引用 · strong 边 · 根 `../`

`ax.power` 幂集公理 → `def.power-set` 幂集 𝒫(X)

**幂集公理**才是「$\mathcal{P}(X)$ 是一个集合」的依据 —— 陈述里那句「由幂集公理，$\mathcal{P}(X)$ 确实是集合」说的就是它。
