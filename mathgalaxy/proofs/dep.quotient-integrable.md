# dep.quotient-integrable
`dep.quotient-integrable` · 定义引用 · strong 边 · 根 `../`

`def.quotient-set` 商集与等价类 → `def.integrable` 可积 / L¹

$L^1$ 的元素不是函数本身，而是**函数在 a.e. 相等下的等价类** —— 后面 $L^p$、RN 导数用的是同一个手法。
