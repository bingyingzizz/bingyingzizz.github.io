# def-link.product-base
`def-link.product-base` · 定义引用 · strong 边 · 根 `../`

`def.product-sigma` 积 σ-代数 → `prop.product-sigma-base` 积 σ-代数的生成基

这条命题说的就是把每一维的 $\mathcal{M}_\alpha$ 换成生成元后，积 $\sigma$代数不变。
