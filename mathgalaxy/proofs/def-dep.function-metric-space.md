# def-dep.function-metric-space
`def-dep.function-metric-space` · 定义引用 · strong 边 · 根 `../`

`def.function` 函数 → `def.metric-space` 距离空间

距离 $d : X \times X \to [0, +\infty)$ 是一个**函数**，记号 $d(x, y)$ 用的就是函数值。
