# dep.cantor-uncountable
`dep.cantor-uncountable` · 定义引用 · strong 边 · 根 `../`

`thm.cantor` Cantor 定理 → `thm.real-uncountable` ℝ 不可数

后半句 $2^{\aleph_0} > \aleph_0$ 用的就是 Cantor 定理（$|A| < |\mathcal{P}(A)|$）。
