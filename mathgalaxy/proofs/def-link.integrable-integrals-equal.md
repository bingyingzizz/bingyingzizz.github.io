# def-link.integrable-integrals-equal
`def-link.integrable-integrals-equal` · 定义引用 · strong 边 · 根 `../`

`def.integrable` 可积 / L¹ → `prop.integrals-equal-iff` 何时两个函数积分处处相同

前提 $f, g \in L^1(\mu)$ 说的是两个函数都**可积**。
