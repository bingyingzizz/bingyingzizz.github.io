# def-dep.measurable-fn-locally-integrable
`def-dep.measurable-fn-locally-integrable` · 定义引用 · strong 边 · 根 `../`

`def.measurable-function` 可测函数 → `def.locally-integrable` 局部可积

局部可积的对象是**可测函数** $f : \mathbb{R}^n \to \mathbb{C}$。
